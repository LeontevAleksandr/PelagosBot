name: deploy-production
on:
  push:
    branches:
      - production
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger deployment
        run: |
          curl -f -s -S --max-time 30 "http://cx41.pelagos.ru/trigger/update-new-bot/5cd774bd4acf20b1e131455d03b5a8db/"
          echo "✅ Deployment webhook triggered"
